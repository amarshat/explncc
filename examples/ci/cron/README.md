# Cron / nightly CI examples (Chapter 12)

| File | Purpose |
|------|---------|
| `run_nightly_report.sh` | Timestamped Markdown + JSON + manifest |
| `example.cron` | Sample crontab entry |

Set `OPT_YAML` to your latest `.opt.yaml`. Default nightly script uses `--explain-backend rule` for Markdown and `--no-explain` for JSON.

See [docs/chapter-12-ci.md](../../../docs/chapter-12-ci.md).
