# unroll_unknown_trip

Trip count derived from runtime data (e.g. loaded from memory). The compiler must assume **unknown** iterations; full unroll is typically unavailable and remarks reflect conservative choices.

Contrast with `unroll_fixed_trip`.
