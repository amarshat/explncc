# unroll_fixed_trip

Small **fixed** trip count (e.g. constant bound known at compile time). Loop unrolling and related transforms have explicit trip-count information.

Use `explncc stats` to see how often loop passes fire on this TU.
